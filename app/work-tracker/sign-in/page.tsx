"use client";
import {useState} from 'react';import '../tracker.css';
export default function SignIn(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/work-tracker/session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'login',email,password})});const b=await r.json() as {error?:string};if(!r.ok)throw Error(b.error);window.location.assign('/work-tracker/public')}catch(e){setError(e instanceof Error?e.message:'Could not sign in');setBusy(false)}}return <main className="wt-root" style={{maxWidth:520,margin:'60px auto',padding:24}}><a href="/work-tracker/public">← Public Work Tracker</a><h1>Team sign-in</h1><p>Use your separate Work Tracker password, not your email password.</p><form onSubmit={submit} style={{display:'grid',gap:20,marginTop:24}}><label>Team email<input type="email" autoComplete="username" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@mcciapune.com" style={{width:'100%',padding:12}}/></label><label>Password<input type="password" autoComplete="current-password" required maxLength={200} value={password} onChange={e=>setPassword(e.target.value)} style={{width:'100%',padding:12}}/></label>{error&&<p role="alert">{error}</p>}<button className="wt-primary" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form><p>Ask your administrator to create or reset your account. Public updates do not require sign-in.</p></main>}
