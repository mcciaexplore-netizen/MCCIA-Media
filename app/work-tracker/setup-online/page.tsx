"use client";
import {useState} from 'react';
export default function Setup(){const [password,setPassword]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);return <main style={{maxWidth:520,margin:'60px auto',padding:24}}><h1>Set up online administrator</h1><p>For aarushig@mcciapune.com. Open on the local editor computer while signed in as an administrator.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);try{const r=await fetch('/api/work-tracker/setup-online',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});const b=await r.json() as {error?:string};setMessage(r.ok?'Administrator created. You can sign in on the live website.':b.error||'Setup unavailable.');setPassword('')}catch{setMessage('Setup unavailable.')}finally{setBusy(false)}}}><label>Separate Work Tracker password<input type="password" autoComplete="new-password" required minLength={12} maxLength={200} value={password} onChange={e=>setPassword(e.target.value)} style={{display:'block',padding:12,width:'100%'}}/></label><button disabled={busy}>Create administrator</button></form><p role="status">{message}</p></main>}
